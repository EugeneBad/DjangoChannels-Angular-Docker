apiVersion: v1
kind: Service
metadata:
  name: djch-backend-svc
spec:
  type: NodePort
  ports:
    - name: api-port
      nodePort: 30003
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: djch-backend-pod
  sessionAffinity: ClientIP